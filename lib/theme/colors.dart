import 'package:flutter/material.dart';

class AppColors {
  /// Fond général des écrans
  static const Color background   = Color(0xFFE1DED5);
  /// Couleur principale (ex. AppBar, titres)
  static const Color primary      = Color(0xFF5D4C3B);
  /// Couleur des cartes, conteneurs secondaires
  static const Color surface      = Color(0xFFF3EEE1);
  /// Variante claire pour textes ou icônes sur background
  static const Color onBackground = Color(0xFF5D4C3B);
  /// Texte principal sur primary
  static const Color onPrimary    = Color(0xFFF3EEE1);
}
